//! User — identity resource under the IAM grouping.
//!
//! Schema-only placeholder today. The `.surql` in this directory defines
//! the `user` table; store, handlers, and CLI wiring will land in a later
//! ticket. This `mod.rs` exists so the schema registry has a stable home
//! for the `include_str!` path and so the module tree mirrors the layer
//! hierarchy exactly.
