# Nauka requests an 8 MiB buffer for each QUIC data-plane socket. Linux
# accounts roughly twice the requested value, so the kernel ceiling must be
# at least 16 MiB or the socket is silently clamped to a few hundred KiB.
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216
